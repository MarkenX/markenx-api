spring:
  application:
    name: ${APP_NAME:markenx-api}

  profiles:
    default: ${SPRING_PROFILES_DEFAULT:dev}

server:
  port: ${SERVER_PORT:8080}
  servlet:
    context-path: ${CONTEXT_PATH:/api/v1}

email:
  allowed-domains: "udla.edu.ec"
